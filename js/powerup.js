export class PowerupManager {
    constructor(assets, canvas, ctx) {
        this.assets = assets;
        this.canvas = canvas;
        this.ctx = ctx;
        this.powerups = [];
    }
    update(dt, player, effects) {
        // no-op placeholder
    }
    render(ctx) {
        // no-op placeholder
    }
}
